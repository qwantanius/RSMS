package RSMS.storage;

public class Storage {
    public static final String STORAGE_DIR = System.getProperty("user.dir") + "/storage/";
    public static final String STORAGE_DIR_LOGS = System.getProperty("user.dir") + STORAGE_DIR + "logs/";
}
